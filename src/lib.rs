pub mod commands;
pub mod driver;
pub mod instruction;
pub mod packet_handler;
pub mod packets;
pub mod serial;
pub mod utils;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
